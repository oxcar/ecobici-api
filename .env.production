# Variables de entorno para produccion
# Descomentar y modificar solo lo que necesites cambiar de los valores por defecto

# Directorio base de datos (default: /app/data)
# Todas las rutas se derivan automaticamente:
# - /app/data/models (modelos XGBoost y LSTM)
# - /app/data/gbfs (snapshots cada minuto)
# - /app/data/statistics (estadisticas de uso)
# - /app/data/cache (cache de consultas)
# DATA_PATH=/app/data

# Nivel de registro de logs (default: INFO)
# Opciones: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

# CORS - Dominios permitidos para hacer peticiones a la API (default: ["https://ecobici.cc","https://www.ecobici.cc"])
# Formato JSON array. Para desarrollo local agregar: ["https://ecobici.cc","https://www.ecobici.cc","http://localhost:3000"]
# CORS_ORIGINS=["https://ecobici.cc","https://www.ecobici.cc"]

# Activar recolector automatico de datos GBFS (default: true)
# GBFS_COLLECTOR_ENABLED=true

# Timeout para peticiones GBFS en segundos (default: 10)
# GBFS_TIMEOUT=10

# Timeout para peticiones de clima en segundos (default: 10)
# OPENMETEO_TIMEOUT=10
